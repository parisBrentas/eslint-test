import { PrismaResetSchema } from './schema';
declare const runExecutor: (options: PrismaResetSchema, context?: import("@nrwl/devkit").ExecutorContext) => Promise<{
    success: true;
}>;
export default runExecutor;
