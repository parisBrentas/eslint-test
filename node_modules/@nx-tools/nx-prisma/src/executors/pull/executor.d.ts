import { PrismaPullSchema } from './schema';
declare const runExecutor: (options: PrismaPullSchema, context?: import("@nrwl/devkit").ExecutorContext) => Promise<{
    success: true;
}>;
export default runExecutor;
