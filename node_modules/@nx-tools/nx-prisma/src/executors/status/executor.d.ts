import { PrismaStatusSchema } from './schema';
declare const runExecutor: (options: PrismaStatusSchema, context?: import("@nrwl/devkit").ExecutorContext) => Promise<{
    success: true;
}>;
export default runExecutor;
