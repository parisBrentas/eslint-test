import { PrismaGenerateSchema } from './schema';
declare const runExecutor: (options: PrismaGenerateSchema, context?: import("@nrwl/devkit").ExecutorContext) => Promise<{
    success: true;
}>;
export default runExecutor;
