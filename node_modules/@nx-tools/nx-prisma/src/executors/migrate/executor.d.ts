import { PrismaMigrateSchema } from './schema';
declare const runExecutor: (options: PrismaMigrateSchema, context?: import("@nrwl/devkit").ExecutorContext) => Promise<{
    success: true;
}>;
export default runExecutor;
