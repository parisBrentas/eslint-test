export interface PrismaBuilderOptions {
    schema?: string;
    options?: Record<string, boolean>;
}
export interface PrismaCommands<T extends PrismaBuilderOptions> {
    description: string;
    command: string;
}
export declare const runCommand: <T extends PrismaBuilderOptions>({ description, command }: PrismaCommands<T>, options: T) => Promise<{
    success: true;
}>;
