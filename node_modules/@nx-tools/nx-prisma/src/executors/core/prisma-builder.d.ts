import { ExecutorContext } from '@nrwl/devkit';
import { PrismaBuilderOptions, PrismaCommands } from './run-commands';
export declare const createPrismaBuilder: <T extends PrismaBuilderOptions>(command: PrismaCommands<T>) => (options: T, context?: ExecutorContext) => Promise<{
    success: true;
}>;
