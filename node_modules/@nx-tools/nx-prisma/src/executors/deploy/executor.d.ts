import { PrismaDeploySchema } from './schema';
declare const runExecutor: (options: PrismaDeploySchema, context?: import("@nrwl/devkit").ExecutorContext) => Promise<{
    success: true;
}>;
export default runExecutor;
